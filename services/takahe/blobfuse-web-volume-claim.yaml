kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nfsdata-web
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: nfsdata-web
  storageClassName: azureblobfuse-standard
